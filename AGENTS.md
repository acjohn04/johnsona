<!-- BEGIN:svelte5-agent-rules -->
# This is Svelte 5

This project uses Svelte 5. Use Runes (`$state`, `$derived`, `$props`, `$effect`) instead of Svelte 4's `let` reassignments, `$:`, `export let`, and lifecycle hooks.
<!-- END:svelte5-agent-rules -->
